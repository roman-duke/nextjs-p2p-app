import { Inter } from 'next/font/google'
import { Hind } from 'next/font/google'

export const inter = Inter({ subsets: ['latin'] })

export const hind = Hind({ weight: ['300', '400', '500'], subsets: ['latin'] })
